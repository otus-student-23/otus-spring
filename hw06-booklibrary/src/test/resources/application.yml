spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: always
      data-locations: data.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
    #properties:
    #  hibernate:
    #    show_sql: true
    #    format_sql: true
  shell:
    interactive:
      enabled: false