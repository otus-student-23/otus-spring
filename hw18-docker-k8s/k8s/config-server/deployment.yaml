apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-config-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: k8s-config-server
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: k8s-config-server
    spec:
      containers:
        - image: docker.io/library/k8s-config-server-image:0.0.22
          name: k8s-config-server
          ports:
            - containerPort: 8888
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /actuator/health/readiness
              port: 8888
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 1
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /actuator/health/liveness
              port: 8888
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 1
            initialDelaySeconds: 10
          imagePullPolicy: Never
      #imagePullSecrets:
        #- name: regcred
